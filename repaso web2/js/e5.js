//modularidad
function suma() //introduccion de funciones
{
    var a,b,s;
    a=parseInt(prompt("a;"));
    b=parseInt(prompt("b;"));
    s=a+b

    alert(s); //se invoca dentro dentro sw un boton
}
